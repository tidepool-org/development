apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert
    kompose.version: 1.17.0 (a74acad)
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v1
      kind:  Mapping
      ambassador_id: {{ .Values.ambassador.id }}
      host: {{ include "charts.host.api" . }}
      name: {{ .Release.Namespace -}}-message-api-message
      prefix: /message/
      method: "GET|OPTIONS|POST|PUT|PATCH|DELETE"
      method_regex: true
      path_redirect: "false"
      rewrite: "/"
      service: message-api.{{ .Release.Namespace }}:{{.Values.messageapi.port}}
  creationTimestamp: null
  labels:
    io.kompose.service: message-api
    helm.sh/chart: {{ include "charts.chart" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: message-api
  namespace: {{.Release.Namespace}}
spec:
  ports:
  - name: "{{.Values.messageapi.port}}"
    port: {{.Values.messageapi.port}}
    targetPort: {{.Values.messageapi.port}}
  selector:
    io.kompose.service: message-api
    app.kubernetes.io/name: {{ include "charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
status:
  loadBalancer: {}
