apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert
    kompose.version: 1.17.0 (a74acad)
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v1
      kind:  Mapping
      host: {{ include "charts.hostname" . }}
      name: {{ .Release.Namespace -}}-hydrophone-confirm
      prefix: /confirm/
      method: "GET|OPTIONS|POST|PUT|PATCH|DELETE"
      method_regex: true
      rewrite: "/"
      path_redirect: "false"
      service: hydrophone.{{ .Release.Namespace }}:{{.Values.hydrophone.port}}
  creationTimestamp: null
  labels:
    io.kompose.service: hydrophone
    helm.sh/chart: {{ include "charts.chart" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: hydrophone
  namespace: {{.Release.Namespace}}
spec:
  ports:
  - name: "{{.Values.hydrophone.port}}"
    port: {{.Values.hydrophone.port}}
    targetPort: {{.Values.hydrophone.port}}
  selector:
    io.kompose.service: hydrophone
    app.kubernetes.io/name: {{ include "charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
status:
  loadBalancer: {}
