apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert
    kompose.version: 1.17.0 (a74acad)
  creationTimestamp: null
  labels:
    io.kompose.service: platform-blob
  name: platform-blob
  namespace: {{.Release.Namespace}}
spec:
  {{ include "charts.init.shoreline" .}}
  selector:
    matchLabels:
      io.kompose.service: platform-blob
  replicas: 1
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: platform-blob
    spec:
      containers:
      - env:
        {{ include "charts.platform.env" .}}
        image: "{{ .Values.platformBlob.image }}"
        {{ template "charts.platform.probes" .Values.platformBlob}}
        name: platform-blob
        ports:
        - containerPort: {{.Values.platformBlob.port}}
        resources: {}
      restartPolicy: Always
status: {}
