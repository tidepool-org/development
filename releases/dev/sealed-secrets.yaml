---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: aa-sealed-secrets
  namespace: kube-system
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.chart-image: glob:*
spec:
  releaseName: ab-sealed-secrets
  chart:
    git: git@github.com:helm/charts
    path: charts/stable/sealed-secrets
    ref: master
  values:
    rbac.create: true
