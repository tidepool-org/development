#!/bin/bash
# identity the clusters that we can connect to from the current kubeconfig that match the input string
# matching means that the input is a prefix of the cluster name

clusters=$(kubectl config get-clusters | tail -n +2)
for name in $clusters
do
   if [[ $name == "$1"* ]]
   then
       echo $name
   fi
done
