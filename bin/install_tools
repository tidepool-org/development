#!/bin/bash 
#
# install standard tools needed to create/install cluster
#
# Usage: $0

if [ -z "$DEV_REPO" ]
then
	echo "\$DEV_REPO required."
	exit
fi

brew bundle --file=${DEV_REPO}/Brewfile
pip3 install boto3 --user
