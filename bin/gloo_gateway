#!/bin/bash

mappings | gloo_manifests http  | single_file "{{ if and (.Values.gloo.enabled) (.Values.http.enabled) }}" > "gloo-http.yaml"
mappings | gloo_manifests https | single_file "{{ if and (.Values.gloo.enabled) (.Values.https.enabled) }}" > "gloo-https.yaml"

