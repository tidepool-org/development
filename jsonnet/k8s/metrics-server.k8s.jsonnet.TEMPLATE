local aws = import 'aws.jsonnet';

function(config) (
  local group = config.groups.metricsServer;
  if group.enabled then {
    Helmrelease: if group.helmrelease.create then aws.helmrelease(config, group),
    Namespace: if group.namespace.create then aws.namespace(config, group),
  }
)
