apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: mongo
  namespace: int
secretDescriptor:
  backendType: secretsManager
  data:
  - key: int/mongo
    name: password
    property: password

