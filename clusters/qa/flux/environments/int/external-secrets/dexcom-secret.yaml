apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: dexcom
  namespace: int
secretDescriptor:
  backendType: secretsManager
  data:
  - key: int/dexcom
    name: CLIENT_ID
    property: CLIENT_ID
  - key: int/dexcom
    name: CLIENT_SECRET
    property: CLIENT_SECRET
  - key: int/dexcom
    name: STATE_SALT
    property: STATE_SALT

