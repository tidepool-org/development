apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: dexcom
  namespace: dev
secretDescriptor:
  backendType: secretsManager
  data:
  - key: dev/dexcom
    name: CLIENT_ID
    property: CLIENT_ID
  - key: dev/dexcom
    name: CLIENT_SECRET
    property: CLIENT_SECRET
  - key: dev/dexcom
    name: STATE_SALT
    property: STATE_SALT

