apiVersion: v1
kind: Namespace
metadata:
  name: autoscaler
  labels:
    istio-injection: disabled
    linkerd.io/inject: disabled
  annotations:
    "helm.sh/hook": "pre-install"
    iam.amazonaws.com/permitted: "k8s-autoscaler"
