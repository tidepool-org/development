apiVersion: v1
kind: Namespace
metadata:
  name: external-secrets
  labels:
    istio-injection: disabled
    linkerd.io/inject: enabled
  annotations:
    "helm.sh/hook": "pre-install"
    iam.amazonaws.com/role: external-secrets
