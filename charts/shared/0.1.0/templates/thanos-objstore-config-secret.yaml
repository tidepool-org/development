{{ if .Values.thanos.secret.create }}
---
apiVersion: v1
data:
  thanos.yaml: {{ include "charts.thanos.secret" . | b64enc }}
kind: Secret
metadata:
  name: {{ .Values.thanos.secret.name }}
  namespace: {{ .Values.thanos.namespace.name }}
type: Opaque
{{ end }}
