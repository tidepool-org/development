{{ if (eq .Values.global.secrets.source "awsSecretsManager") }}
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: sumologic
  namespace: sumologic
secretDescriptor:
  backendType: secretsManager
  data:
  - key: development/sumologic/sumologic
    name: collector-url
    property: collector-url
{{ end }}:w

