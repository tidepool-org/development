{{ if .Values.sumologic-fluentd.enabled }}
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: sumologic
  namespace: sumologic
secretDescriptor:
  backendType: secretsManager
  data:
  - key: sumologic/sumologic
    name: collector-url
    property: collector-url
{{ end }}
