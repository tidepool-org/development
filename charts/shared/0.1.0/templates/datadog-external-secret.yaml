apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: datadog
  namespace: datadog
secretDescriptor:
  backendType: secretsManager
  data:
  - key: development/datadog/datadog
    name: api-key
    property: api-key

