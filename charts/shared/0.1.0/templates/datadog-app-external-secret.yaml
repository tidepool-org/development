apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: datadog-app
  namespace: datadog
secretDescriptor:
  backendType: secretsManager
  data:
  - key: development/datadog/datadog-app
    name: app-key
    property: app-key

