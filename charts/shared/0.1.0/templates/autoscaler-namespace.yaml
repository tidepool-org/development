{{ if .Values.autoscaler.namespace.create }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: autoscaler
  labels:
{{ include "charts.mesh.labels" . }}
  annotations:
    iam.amazonaws.com/permitted: {{ include "charts.autoscaler.role" . }}
{{ end }}
  
