apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: flux-slack
  namespace: flux
secretDescriptor:
  backendType: secretsManager
  data:
  - key: flux/flux-slack
    name: values.yaml
    property: values.yaml

