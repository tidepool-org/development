apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: slack
  namespace: flux
secretDescriptor:
  backendType: secretsManager
  data:
  - key: development/flux/slack
    name: values.yaml
    property: values.yaml
