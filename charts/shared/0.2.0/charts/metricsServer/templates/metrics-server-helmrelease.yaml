---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: metrics-server
  namespace: metrics-server
  annotations:
    flux.weave.works/automated: "false"
spec:
  releaseName: metrics-server
  chart:
    {{- .Values.chart | toYaml | nindent 4 }}
  values: 
    hostNetwork:
      enabled: true
