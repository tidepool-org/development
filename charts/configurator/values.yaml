global:
  email: derrick@tidepool.org
  repo:
    directory: "/tmp/configrepo"                # REQUIRED: where to create the config repo
    name: "cluster-badass"                      # OPTIONAL: name of the repo
    create: true                                # whether to create the config repo  directory
    branch: "master"                            # branch to monitor
  github:
    account: tidepool-org                       # your GitHub account
    user: derrickburns                          # your GitHub user name
    push: false                                 # whether to push the completed repo to GitHub
    token: ""                                   # a GitHub token that allows repo read/write access  
  logLevel: debug                               # the default log level for all services
  iamUsers: "benderr-cli lennartgoedhart-cli""  # the list of IAM users to provide master cluster access-
  cluster:
    name: "badass"                              # the name of the cluster
    kubeconfig: ""                              # place to put KUBECONFIG, default: $CLUSTER_DIR/kubeconfig.yaml
    configFile: ""                              # place to put cluster config, default $CLUSTER_DIR/config.yaml
    k8sVersion: "1.13"                          # the Kubernetes version >= 1.13
    region: "us-west-2"                         # AWS region to deploy in
    cidr: "10.47.0.0/16"                        # CIDR of AWS VPC
    nodegroup:
      instanceType: "m4.large"                  # AWS instance type for workers 
      desiredCapacity: 3                        # initial capacity of auto scaling group of workers
      minSize: 1                                # minimum size of auto scaling group of workers
      maxSize: 10                               # maximum size of auto scaling group of workers
  hostnames: []                                 # list of hosts to publish to DNS
sumologic:
  enabled: false
  secrets:
    CollectorUrl:  ""                           # Sumologic collector URL
datadog:
  enabled: false
  secrets:
    APIKey: ""                                  # Datadog API key
    AppKey: ""                                  # Datadog Application key
fluxcloud:
  enabled: false
  secrets:
    url: ""                                     # Slack Webhook url
env:
  qa1:
    mongo:                                      # MongoDB connection parameters
      scheme: "mongodb"
      addresses: "mongodb"
      username: ""
      password: ""
      database: ""
      tls: "true"
      optParams: ""
    hosts:
      default:
        protocol: https                         # protocol to use for email signups
      http:
        enabled: false
        dnsNames:                               # list of DNS names supporting HTTP access on port 80
        - badass.tidepool.org
      https:
        enabled: false
        dnsNames:                               # list of DNS names supporting HTTPS access on port 443
        - badass.tidepool.org
        certificateIssuer: letsencrypt-staging   # certificate issuer, set to "" for no certificates
    gitops:
      branch: develop                           # branch to pull updates from
    auth:
      secrets:
        ServiceAuth:  ""				
    blob:
      secrets:
        ServiceAuth: ""				
    data:
      secrets:
        ServiceAuth: ""				
    task:
      secrets:
        ServiceAuth: ""				
    image:
      secrets:
        ServiceAuth: ""				
    user:
      secrets:
        ServiceAuth: ""				
    gatekeeper:
      secrets:
        ServiceAuth: ""				
    shoreline:
      secrets:
        ServiceAuth: ""				
        UserLongTermKey: 
        UserMailVerification: ""
        UserPasswordSalt: ""
        ClinicDemoUserId: ""
    tidepoolServerSecret:
      secrets:
        ServiceAuth: ""				
    highwater:
      secrets:
        UserIdSalt: ""
    notification:
      secrets:
        ServiceAuth: ""				
    export:
      secrets:
        SessionEncryptionKey: ""
    dexcom:
      enabled: false
      secrets:
        ClientId: ""
        ClientSecret: ""
        StateSalt: ""
    carelink:
      enabled: false
      secrets:
        CareLinkSalt: ""
    nosqlclient:
      enabled: true

