apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f docker-compose.yml convert
    kompose.version: 1.17.0 (a74acad)
  creationTimestamp: null
  labels:
    io.kompose.service: platform-notification
  name: platform-notification
spec:
  ports:
  - name: "{{.Values.plaformNotification.port}}"
    port: {{.Values.plaformNotification.port}}
    targetPort: {{.Values.plaformNotification.port}}
  selector:
    io.kompose.service: platform-notification
status:
  loadBalancer: {}
