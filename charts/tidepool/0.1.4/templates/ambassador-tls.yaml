{{ if  (and (eq .Values.global.gateway.name "ambassador")  (.Values.global.gateway.https.enabled))}}
apiVersion: ambassador/v1
kind: TLSContext
name: tidepool-org-tlscontext
hosts:
- "*.tidepool.org"
secret: tidepool-org
{{ end }}
