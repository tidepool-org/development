---
apiVersion: v1
kind: Service
metadata:
  name: internal 
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard" .}}
spec:
  externalName: internal-gateway-proxy.{{- .Values.ingress.deployment.namespace -}}.svc.cluster.local
  ports:
  - port: 80
  type: ExternalName
