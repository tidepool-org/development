apiVersion: v1
kind: Service
metadata:
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: tidepool/v1
      kind:  Mapping
      host: {{ include "charts.host.api" . }}
      name: {{ .Release.Namespace -}}-platform-task-tasks
      prefix: "/v1/tasks"
      method: "GET|POST"
      method_regex: true
      rewrite: ""
      service: platform-task.{{ .Release.Namespace }}:{{.Values.platformTask.port}}
      ---
      apiVersion: tidepool/v1
      kind:  Mapping
      host: {{ include "charts.host.api" . }}
      name: {{ .Release.Namespace -}}-platform-task-tasks-id
      prefix: "/v1/tasks/[^/*]"
      prefix_regex: true
      method: "GET|PUT|DELETE"
      method_regex: true
      rewrite: ""
      service: platform-task.{{ .Release.Namespace }}:{{.Values.platformTask.port}}
  labels:
    app: platform-task
    helm.sh/chart: {{ include "charts.chart" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: platform-task
  namespace: {{.Release.Namespace}}
spec:
  ports:
  - name: "{{.Values.platformTask.port}}"
    port: {{.Values.platformTask.port}}
    targetPort: {{.Values.platformTask.port}}
  selector:
    app: platform-task
    app.kubernetes.io/name: {{ include "charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
status:
  loadBalancer: {}
