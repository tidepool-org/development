{{ if .Values.jellyfish.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: jellyfish
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.jellyfish.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.jellyfish.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: jellyfish
  targetCPUUtilizationPercentage: {{ .Values.jellyfish.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
