{{ if .Values.blip.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: blip
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.blip.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.blip.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: blip
  targetCPUUtilizationPercentage: {{ .Values.blip.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
