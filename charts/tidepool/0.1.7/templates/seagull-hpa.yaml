{{ if .Values.seagull.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: seagull
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.seagull.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.seagull.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: seagull
  targetCPUUtilizationPercentage: {{ .Values.seagull.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
