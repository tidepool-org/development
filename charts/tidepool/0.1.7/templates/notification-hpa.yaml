{{ if .Values.notification.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: notification
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.notification.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.notification.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: notification
  targetCPUUtilizationPercentage: {{ .Values.notification.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
