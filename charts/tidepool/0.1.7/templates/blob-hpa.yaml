{{ if .Values.blob.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: blob
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.blob.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.blob.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: blob
  targetCPUUtilizationPercentage: {{ .Values.blob.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
