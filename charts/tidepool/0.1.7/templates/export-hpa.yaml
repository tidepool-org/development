{{ if .Values.export.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: export
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.export.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.export.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: export
  targetCPUUtilizationPercentage: {{ .Values.export.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
