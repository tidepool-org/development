{{ if .Values.export.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: export
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard" .}}
spec:
  maxReplicas: {{ .Values.export.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.export.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: export
  targetCPUUtilizationPercentage: {{ .Values.export.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
