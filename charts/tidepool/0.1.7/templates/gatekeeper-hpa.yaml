{{ if .Values.gatekeeper.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: gatekeeper
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.gatekeeper.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.gatekeeper.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: gatekeeper
  targetCPUUtilizationPercentage: {{ .Values.gatekeeper.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
