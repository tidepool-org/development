{{ if .Values.auth.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: auth
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.auth.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.auth.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: auth
  targetCPUUtilizationPercentage: {{ .Values.auth.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
