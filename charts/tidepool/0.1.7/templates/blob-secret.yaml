{{ if .Values.blob.secret.create -}}
---
apiVersion: v1
data:
  ServiceAuth: {{ .Values.blob.secret.data_.ServiceAuth | default (randAlphaNum 40) | b64enc }}
kind: Secret
metadata:
  name: blob
  namespace: {{ .Release.Namespace }}
  labels:
    app: blob
{{ include "charts.labels.standard .}}
type: Opaque
{{- end }}
