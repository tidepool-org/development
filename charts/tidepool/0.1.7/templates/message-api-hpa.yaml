{{ if .Values.message-api.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: messageapi
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.message-api.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.message-api.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: messageapi
  targetCPUUtilizationPercentage: {{ .Values.message-api.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
