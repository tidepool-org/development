{{ if .Values.shoreline.configmap.create -}}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: shoreline
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
data:
  ClinicDemoUserId: {{ .Values.shoreline.configmap.data.ClinicDemoUserId | quote }}
{{- end }}
