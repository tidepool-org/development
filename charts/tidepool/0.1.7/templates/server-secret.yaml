{{ if .Values.server.secret.create -}}
---
apiVersion: v1
data:
  ServiceAuth: {{ .Values.server.secret.data_.ServiceAuth | default (randAlphaNum 40) | b64enc }}
kind: Secret
metadata:
  name: server
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard" .}}
type: Opaque
{{- end }}
