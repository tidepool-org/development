{{ if .Values.hydrophone.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: hydrophone
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard" .}}
spec:
  maxReplicas: {{ .Values.hydrophone.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.hydrophone.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: hydrophone
  targetCPUUtilizationPercentage: {{ .Values.hydrophone.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
