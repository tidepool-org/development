---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: task
{{ include "charts.labels.standard" .}}
  name: task
  namespace: {{.Release.Namespace}}
  annotations:
    secret.reloader.stakater.com/reload: "server,mongo,task"
spec:
  selector:
    matchLabels:
      app: task
      app.kubernetes.io/name: {{ include "charts.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  replicas: 1
  strategy: {}
  template:
    metadata:
      labels:
        app: task
        app.kubernetes.io/name: {{ include "charts.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
{{ if .Values.task.podAnnotations }}
      annotations:
        {{- .Values.task.podAnnotations | toYaml | nindent 8 }}
{{- end }}
    spec:
      initContainers:
      {{ include "charts.init.shoreline" .}}
      containers:
      - env:
        {{ include "charts.platform.env.mongo" .}}
        {{ include "charts.platform.env.misc" .}}
        {{ include "charts.platform.env.clients" .}}
        - name: TIDEPOOL_TASK_QUEUE_DELAY
          value: {{ .Values.task.deployment.env.queue.delay | quote }}
        - name: TIDEPOOL_TASK_QUEUE_WORKERS
          value: {{ .Values.task.deployment.env.queue.workers | quote }}
        - name: TIDEPOOL_TASK_SERVICE_SECRET
          valueFrom:
            secretKeyRef:
              name: task
              key: ServiceAuth
        - name: TIDEPOOL_TASK_SERVICE_SERVER_ADDRESS
          value: :80
        image: "{{ .Values.task.deployment.image }}"
        {{ template "charts.platform.probes" . }}
        name: task
        ports:
        - containerPort: 80
        resources:
          {{- toYaml .Values.task.resources | nindent 10 }}
        securityContext:
          {{- toYaml .Values.task.securityContext | nindent 10 }}
      restartPolicy: Always
