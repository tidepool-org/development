{{ if .Values.data.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: data
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.data.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.data.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: data
  targetCPUUtilizationPercentage: {{ .Values.data.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
