{{ if .Values.data.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: data
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.data.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.data.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: data
  targetCPUUtilizationPercentage: {{ .Values.data.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
