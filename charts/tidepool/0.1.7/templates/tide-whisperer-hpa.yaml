{{ if .Values.tide-whisperer.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: tide-whisperer
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.tide-whisperer.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.tide-whisperer.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: tide-whisperer
  targetCPUUtilizationPercentage: {{ .Values.tide-whisperer.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
