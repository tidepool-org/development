{{ if .Values.user.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: user
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.user.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.user.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: user
  targetCPUUtilizationPercentage: {{ .Values.user.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
