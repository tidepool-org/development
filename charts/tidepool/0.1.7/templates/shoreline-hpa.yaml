{{ if .Values.shoreline.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: shoreline
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.shoreline.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.shoreline.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: shoreline
  targetCPUUtilizationPercentage: {{ .Values.shoreline.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
