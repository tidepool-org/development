{{ if .Values.highwater.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: highwater
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.highwater.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.highwater.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: highwater
  targetCPUUtilizationPercentage: {{ .Values.highwater.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
