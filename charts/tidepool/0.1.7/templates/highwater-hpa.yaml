{{ if .Values.highwater.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: highwater
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.highwater.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.highwater.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: highwater
  targetCPUUtilizationPercentage: {{ .Values.highwater.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
