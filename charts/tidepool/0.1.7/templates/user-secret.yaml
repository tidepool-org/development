{{ if .Values.user.secret.create -}}
---
apiVersion: v1
data:
  ServiceAuth: {{ .Values.user.secret.data_.ServiceAuth | default (randAlphaNum 40) | b64enc }}
kind: Secret
metadata:
  name: user
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
type: Opaque
{{- end }}
