{{ if .Values.tools.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: tools
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.tools.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.tools.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: tools
  targetCPUUtilizationPercentage: {{ .Values.tools.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
