{{ if .Values.carelink.secret.create -}}
---
apiVersion: v1
data:
  CarelinkSalt: {{ .Values.carelink.secret.data_.CarelinkSalt | default "" | b64enc  }}
kind: Secret
metadata:
  name: jellyfish
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
type: Opaque
{{- end }}
