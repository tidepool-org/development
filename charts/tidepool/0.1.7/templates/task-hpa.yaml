{{ if .Values.global.cluster.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: task
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.global.cluster.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.global.cluster.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: task
  targetCPUUtilizationPercentage: {{ .Values.global.cluster.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
