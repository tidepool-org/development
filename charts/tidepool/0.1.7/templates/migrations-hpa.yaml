{{ if .Values.migrations.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: migrations
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.migrations.hpa.maxReplicas | default 10 }}
  minReplicas: {{ .Values.migrations.hpa.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: migrations
  targetCPUUtilizationPercentage: {{ .Values.migrations.hpa.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
