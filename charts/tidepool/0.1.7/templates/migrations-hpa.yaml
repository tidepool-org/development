{{ if .Values.migrations.hpa.create -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: migrations
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard .}}
spec:
  maxReplicas: {{ .Values.migrations.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.migrations.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: migrations
  targetCPUUtilizationPercentage: {{ .Values.migrations.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
