{{ if .Values.secret.enabled -}}
---
apiVersion: v1
data:
  ServiceAuth: {{ .Values.secret.data_.ServiceAuth | default (randAlphaNum 40) | b64enc }}
kind: Secret
metadata:
  name: task
  namespace: {{ .Release.Namespace }}
  labels:
    app: task
{{ include "charts.labels.standard" .}}
type: Opaque
{{- end }}
