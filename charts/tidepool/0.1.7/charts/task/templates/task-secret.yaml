{{ if .Values.task.secret.create -}}
---
apiVersion: v1
data:
  ServiceAuth: {{ .Values.task.secret.data_.ServiceAuth | default (randAlphaNum 40) | b64enc }}
kind: Secret
metadata:
  name: task
  namespace: {{ .Release.Namespace }}
  labels:
    app: task
{{ include "charts.labels.standard" .}}
type: Opaque
{{- end }}
