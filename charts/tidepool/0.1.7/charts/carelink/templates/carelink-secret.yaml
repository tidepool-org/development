{{ if .Values.secret.create -}}
---
apiVersion: v1
data:
  CarelinkSalt: {{ .Values.secret.data_.CarelinkSalt | default "" | b64enc  }}
kind: Secret
metadata:
  name: jellyfish
  namespace: {{ .Release.Namespace }}
  labels:
    app: jellyfish
{{ include "charts.labels.standard" .}}
type: Opaque
{{- end }}
