{{ if .Values.hpa.enabled -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: tools
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard" .}}
spec:
  maxReplicas: {{ .Values.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: tools
  targetCPUUtilizationPercentage: {{ .Values.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
