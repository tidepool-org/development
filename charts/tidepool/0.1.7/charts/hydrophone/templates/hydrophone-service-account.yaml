apiVersion: v1
kind: ServiceAccount
metadata:
  name: hydrophone
  namespace: {{ .Release.Namespace }}
    annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.global.awsAccountId }}:role/{{ .Values.global.clusterName }}-{{.Release.Namespace}}-hydrophone
 
