{{ if .Values.messageapi.hpa.enabled -}}
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  annotations:
  name: messageapi
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "charts.labels.standard" .}}
spec:
  maxReplicas: {{ .Values.messageapi.hpa.data.maxReplicas | default 10 }}
  minReplicas: {{ .Values.messageapi.hpa.data.minReplicas | default 1 }}
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: messageapi
  targetCPUUtilizationPercentage: {{ .Values.messageapi.hpa.data.targetCPUUtilizationPercentage | default 50 }}
{{- end }}
