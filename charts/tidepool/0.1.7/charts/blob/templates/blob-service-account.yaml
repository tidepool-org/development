apiVersion: v1
kind: ServiceAccount
metadata:
  name: blob
  namespace: {{ .Release.Namespace }}
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.global.awsAccountId }}:role/{{ .Values.global.clusterName }}-{{.Release.Namespace}}-blob
 
