apiVersion: v1
kind: ServiceAccount
metadata:
  name: jellyfish
  namespace: {{ .Release.Namespace }}
    annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.global.awsAccountId }}:role/{{ .Values.global.clusterName }}-{{.Release.Namespace}}-jellyfish
