{{ if .Values.global.glooingress.enabled }}
apiVersion: gateway.solo.io/v1
kind: RouteTable
metadata:
  name: export
  namespace: {{ .Release.Namespace }}
  labels:
    namespace: {{ .Release.Namespace }}
    app: tidepool
spec:
  routes:
  - matchers:
    - methods:
      - GET
      exact: /export/status
    routeAction:
      single:
        upstream:
          name: export
          namespace: {{ .Release.Namespace }}
    options:
      jwt:
       disable: true
  - matchers:
    - prefix: /export/
    options:
      timeout: '2m'
    routeAction:
      single:
        upstream:
          name: export
          namespace: {{ .Release.Namespace }}
{{- end }}
