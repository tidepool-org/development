apiVersion: gateway.solo.io/v1
kind: RouteTable
metadata:
  labels:
    app: tidepool
    namespace: dev1
  name: blip
  namespace: dev1
spec:
  routes:
  - matchers:
    - prefix: /
    routeAction:
      single:
        upstream:
          name: blip
          namespace: dev1
