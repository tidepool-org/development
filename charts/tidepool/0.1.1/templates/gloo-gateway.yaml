{{- if .Values.gloo.enabled -}}
apiVersion: gateway.solo.io/v1
bindAddress: '::'
bindPort: 8080
kind: Gateway
metadata:
  name: gateway
  namespace: gloo-system
ssl: false
useProxyProto: false
{{- end -}}
