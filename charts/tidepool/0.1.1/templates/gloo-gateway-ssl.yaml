{{- if .Values.gloo.enabled -}}
apiVersion: gateway.solo.io/v1
bindAddress: '::'
bindPort: 8443
kind: Gateway
metadata:
  name: gateway-ssl
  namespace: gloo-system
ssl: true
useProxyProto: false
{{- end -}}
