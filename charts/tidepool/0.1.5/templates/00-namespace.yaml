{{if .Values.global.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{.Release.Namespace}}
  labels:
    istio-injection: disabled
    linkerd.io/inject: enabled
  annotations:
    iam.amazonaws.com/permitted: {{ .Release.Namespace }}-Worker
{{ end }}

