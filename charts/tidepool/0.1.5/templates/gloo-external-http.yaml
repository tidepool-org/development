# Auto-generated file by gloo_gateway. Do not edit.
{{- $relname := .Release.Name -}}
{{- range $key, $spec := .Values.global.hosts.http }}
--- #file: gloo-{{ $key }}-external-http-virtualservice.yaml
apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: '{{ $key }}-{{ $.Release.Name }}-external-http'
  namespace: '{{ $.Release.Name }}'
spec:
  displayName: '{{ $key }}-{{ $.Release.Name }}-external-http'
  virtualHost:
    domains:
    - '{{ $spec.name }}'
    routes:
    - matcher:
        methods:
        - GET
        - POST
        - DELETE
        regex: /v1/users/[^/]+/restricted_tokens
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-auth-9222'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - POST
        - DELETE
        regex: /v1/users/[^/]+/provider_sessions
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-auth-9222'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /request-password-from-uploader
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - POST
        - DELETE
        - OPTIONS
        regex: /v1/users/[^/]+/data_sources
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - PUT
        - DELETE
        regex: /v1/provider_sessions/[^/]+
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-auth-9222'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /verification-with-password
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - POST
        prefix: /event/csp-report/violation
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - DELETE
        regex: /v1/oauth/[^/]+/authorize
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-auth-9222'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        regex: /v1/users/[^/]+/data_sets
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - POST
        - DELETE
        regex: /v1/users/[^/]+/images/.+
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-image-9226'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        regex: /v1/oauth/[^/]+/redirect
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-auth-9222'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - POST
        regex: /v1/users/[^/]+/datasets
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /request-password-reset
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /confirm-password-reset
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        regex: /v1/blobs/[^/]+/content
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blob-9225'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - PUT
        - DELETE
        regex: /v1/data_sources/[^/]+
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - POST
        - DELETE
        regex: /v1/users/[^/]+/images
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-image-9226'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - PUT
        - DELETE
        prefix: /v1/restricted_tokens
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-auth-9222'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - POST
        - DELETE
        regex: /v1/users/[^/]+/blobs
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blob-9225'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - DELETE
        regex: /v1/users/[^/]+/data
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - POST
        prefix: /v1/device/upload/cl
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-jellyfish-9122'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /email-verification
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /clinician-details
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /v1/device/data/"
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-jellyfish-9122'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /browser-warning
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - DELETE
        regex: /v1/blobs/[^/]+
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blob-9225'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - PUT
        - DELETE
        regex: /v1/tasks/[^/*]
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-task-9224'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - DELETE
        regex: /v1/users/[^/*]
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-user-9221'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /dataservices/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        prefix: /v1/synctasks/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-jellyfish-9122'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /userservices/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-user-9221'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        prefix: /v1/data_sets
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - POST
        - PUT
        - DELETE
        prefix: /v1/datasets
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - PUT
        - DELETE
        prefix: /v1/images
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-image-9226'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /metadata/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-seagull-9120'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        prefix: /patients
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /v1/data/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /metrics/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-highwater-9191'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /confirm/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-hydrophone-9157'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /message/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-message-api-9119'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        - POST
        prefix: /v1/tasks
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-task-9224'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /profile
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /v1/time
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-data-9220'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /export/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-export-9300'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /access/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-gatekeeper-9123'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /signup
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /terms
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - POST
        prefix: /data/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-jellyfish-9122'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
        prefix: /auth/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-shoreline-9107'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        prefix: /data/
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-tide-whisperer-9127'
            namespace: '{{ $.Release.Name }}'
      routePlugins:
        prefixRewrite:
          prefixRewrite: /
    - matcher:
        methods:
        - GET
        prefix: /info
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-jellyfish-9122'
            namespace: '{{ $.Release.Name }}'
    - matcher:
        methods:
        - GET
        prefix: /
      routeAction:
        single:
          upstream:
            name: '{{ $.Release.Name }}-blip-3000'
            namespace: '{{ $.Release.Name }}'
{{ end }}
