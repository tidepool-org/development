{{- if .Values.global.generateSharedSecrets }}
apiVersion: v1
data:
  secret: "{{ randAlphaNum 40 | b64enc }}"
  export: "{{ randAlphaNum 40 | b64enc }}"
  shoreline: "{{ randAlphaNum 40 | b64enc }}"
  gatekeeper: "{{ randAlphaNum 40 | b64enc }}"
  session: "{{ randAlphaNum 40 | b64enc }}"
  auth: "{{ randAlphaNum 40 | b64enc }}"
  blob: "{{ randAlphaNum 40 | b64enc }}"
  data: "{{ randAlphaNum 40 | b64enc }}"
  image: "{{ randAlphaNum 40 | b64enc }}"
  notification: "{{ randAlphaNum 40 | b64enc }}"
  task: "{{ randAlphaNum 40 | b64enc }}"
  user: "{{ randAlphaNum 40 | b64enc }}"
  jellyfish: "{{ randAlphaNum 40 | b64enc }}"
  seagull: "{{ randAlphaNum 40 | b64enc }}"
kind: Secret
type: Opaque
metadata:
  name: server
  namespace: {{ .Release.Namespace }}
  labels:
    helm.sh/chart: {{ include "charts.chart" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
