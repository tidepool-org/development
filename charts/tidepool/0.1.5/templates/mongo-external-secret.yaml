{{ .if .Values.externalSecrets.enabled }}
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: mongo
  namespace: dev
secretDescriptor:
  backendType: secretsManager
  data:
  - key: dev/mongo
    name: password
    property: password
{{ end }}
