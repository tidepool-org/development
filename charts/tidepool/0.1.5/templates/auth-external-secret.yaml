apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: Auth
  namespace: {{ .Release.Namespace }}
secretDescriptor:
  backendType: secretsManager
  data:
  - key: {{ .Release.Namespace }}/Auth
    name: ServiceAuth
    property: ServiceAuth

