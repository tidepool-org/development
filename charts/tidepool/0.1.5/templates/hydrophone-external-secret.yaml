apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: Hydrophone
  namespace: {{ .Release.Namespace }}
secretDescriptor:
  backendType: secretsManager
  data:
  - key: {{ .Release.Namespace }}/Hydrophone
    name: ServiceAuth
    property: ServiceAuth

