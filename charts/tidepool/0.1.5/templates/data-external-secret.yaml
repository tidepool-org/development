apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: Data
  namespace: {{ .Release.Namespace }}
secretDescriptor:
  backendType: secretsManager
  data:
  - key: {{ .Release.Namespace }}/Data
    name: ServiceAuth
    property: ServiceAuth

