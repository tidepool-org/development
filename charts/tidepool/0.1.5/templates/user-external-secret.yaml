{{ if (eq .Values.global.secrets.internal.source "awsSecretsManager") }}
apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: User
  namespace: {{ .Release.Namespace }}
secretDescriptor:
  backendType: secretsManager
  data:
  - key: {{ .Release.Namespace }}/User
    name: ServiceAuth
    property: ServiceAuth

{{ end }}
