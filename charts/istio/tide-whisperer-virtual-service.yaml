apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: tide-whisperer
  namespace: '{{ .Values.namespace }}'
spec:
  gateway:
  - primary-gateway
  hosts:
  - '{{ .Values.api.host }}'
  - '{{ .Values.externalapi.host }}'
  http:
  - match:
    - method:
        regex: GET|OPTIONS|POST|PUT|PATCH|DELETE
      uri:
        prefix: /data/
    rewrite:
      uri: /
    route:
      destination:
        host: tide-whisperer.{{- .Values.namespace -}}.svc.cluster.local
        port:
          number: 9127
