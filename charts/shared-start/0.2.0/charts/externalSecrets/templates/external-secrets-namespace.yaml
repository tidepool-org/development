apiVersion: v1
kind: Namespace
metadata:
  name: external-secrets
  labels:
    istio-injection: disabled
    linkerd.io/inject: disabled
  annotations:
    iam.amazonaws.com/permitted: {{ include "charts.externalSecrets.role" . }}
